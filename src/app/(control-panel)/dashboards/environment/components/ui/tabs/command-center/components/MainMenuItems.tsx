'use client';

import React from 'react';
import { Box } from '@mui/material';

export default function MainMenuItems() {
  return (
    <Box sx={{ 
      width: '100%', 
      height: '100%',
      bgcolor: '#f8fafc',
      p: 0,
      m: 0,
      overflow: 'hidden'
    }}>
      {/* Completely empty for now - we will add content later */}
      <Box sx={{ 
        width: '100%', 
        height: '100%' 
      }} />
    </Box>
  );
}